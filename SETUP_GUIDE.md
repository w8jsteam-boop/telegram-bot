# টেলিগ্রাম চ্যাট বট সেটআপ গাইড 🤖

## ধাপ ১: Token পান
1. টেলিগ্রামে `@BotFather` খুঁজুন
2. `/start` লিখুন
3. `/newbot` কমান্ড দিন
4. বটের নাম দিন
5. একটি unique username দিন (যেমন: `my_bot_2026`)
6. Token কপি করুন (এটি এমন দেখাবে: `123456:ABC-DEF1234567890`)

## ধাপ ২: Replit-এ কোড চালান
1. **replit.com** যান
2. Sign up/Login করুন
3. **Create Repl** ক্লিক করুন
4. Language: **Python** নির্বাচন করুন
5. এই রিপোজিটরি থেকে `bot.py` কোড কপি করুন
6. `bot.py` ফাইলে পেস্ট করুন
7. `requirements.txt` কোড কপি করুন এবং পেস্ট করুন

## ধাপ ৩: Token বসান
`bot.py` ফাইলে এই লাইনটি খুঁজুন:
```python
TOKEN = "আপনার_TOKEN_এখানে_বসাও"
```

`আপনার_TOKEN_এখানে_বসাও` এর জায়গায় BotFather থেকে পাওয়া Token পেস্ট করুন।

**উদাহরণ:**
```python
TOKEN = "123456:ABC-DEF1234567890"
```

## ধাপ ৪: বট চালান
1. Replit-এ **Run** বাটন ক্লিক করুন (সবুজ ত্রিভুজ)
2. Terminal-এ "বট চলছে... 🚀" দেখবেন

## ধাপ ৫: বট টেস্ট করুন
টেলিগ্রামে বট খুঁজুন এবং এই কমান্ডগুলো চেষ্টা করুন:
- `/start` - স্বাগত বার্তা
- `/help` - সাহায্য দেখুন
- `/hello` - হ্যালো বলুন
- `/about` - বটের সম্পর্কে জানুন
- সাধারণ মেসেজ লিখুন - বট রিপ্লাই করবে

## সমস্যা হলে
- Token সঠিক আছে কিনা চেক করুন
- Replit Console-এ কোনো error আছে কিনা দেখুন
- টেলিগ্রামে বটের username খুঁজে পেতে পারছেন কিনা দেখুন

---

**শুভকামনা!** 🚀 যদি কোনো সমস্যা হয় তাহলে জানাবেন।
